export const animationCreate = () => {
  // if (typeof window !== "undefined") {
  //   const WOW = require("wowjs/dist/wow").default; // ✅ usually works
  //   new WOW({ live: false }).init();
  // }
};
